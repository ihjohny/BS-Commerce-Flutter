import 'package:flutter/material.dart';
class CartComponentCard extends StatelessWidget {
  const CartComponentCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
